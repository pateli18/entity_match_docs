Before you can start a job, you need to create a dataset. You can do that in the [App](https://www.entitymatcher.com/datasets) by clicking the `New Dataset` button. Right now the application will only accept CSV files.

<Frame caption="Add a dataset">
  <img src="/screenshots/app/new_dataset.png" alt="Add a dataset" />
</Frame>